{
  "name": "cwn-interface-theme",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "check": "node --check scripts/cwn-interface-theme-v050.mjs && node --check scripts/sheets/cwn-npc-sheet.mjs && node --check tools/stage-release.mjs",
    "test": "node --test tests",
    "stage": "node tools/stage-release.mjs",
    "build": "npm run check && npm test && npm run stage"
  }
}
